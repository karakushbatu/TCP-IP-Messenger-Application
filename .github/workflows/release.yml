name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install -r requirements-dev.txt

      - name: Run Ruff
        run: python -m ruff check src/ tests/

      - name: Run tests with coverage
        run: python -m pytest tests/ -v --cov=src/protocol --cov-report=term-missing --cov-fail-under=90

  build-macos:
    needs: test
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install -r requirements-dev.txt

      - name: Build macOS (DMG + ZIP)
        run: bash build/build_macos.sh

      - name: Upload macOS DMG
        uses: actions/upload-artifact@v4
        with:
          name: TCP-Tactical-Messenger-macOS-dmg
          path: dist/TCP-Tactical-Messenger-macOS.dmg

      - name: Upload macOS ZIP
        uses: actions/upload-artifact@v4
        with:
          name: TCP-Tactical-Messenger-macOS-zip
          path: dist/TCP-Tactical-Messenger-macOS.zip

  build-windows:
    needs: test
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install -r requirements-dev.txt

      - name: Build Windows
        run: build\build_windows.bat

      - name: Upload Windows artifact
        uses: actions/upload-artifact@v4
        with:
          name: TCP-Tactical-Messenger-Windows
          path: dist/TCP-Tactical-Messenger-Windows.zip

  release:
    needs: [build-macos, build-windows]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            artifacts/TCP-Tactical-Messenger-macOS-dmg/TCP-Tactical-Messenger-macOS.dmg
            artifacts/TCP-Tactical-Messenger-macOS-zip/TCP-Tactical-Messenger-macOS.zip
            artifacts/TCP-Tactical-Messenger-Windows/TCP-Tactical-Messenger-Windows.zip
